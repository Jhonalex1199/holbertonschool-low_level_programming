#include "holberton.h"

/**
 * mul - Write a function that multiplies two integers
 * @a: one variable
 * @b: one variable
 * Return: Always mul.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
